# Proxy Service Endpoints

- `POST /api/v1/proxy/rotate` - Rotate to a new outbound proxy
- `GET /api/v1/proxy/status` - Get status of current proxy pools
- `GET /api/v1/proxy/routing` - Get regional routing configuration
