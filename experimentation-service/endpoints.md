# Experimentation Service Endpoints

- `POST /api/v1/experiments` - Create a new A/B or multivariate experiment
- `GET /api/v1/experiments/{id}` - Get status and results of an experiment
- `POST /api/v1/experiments/{id}/stop` - Stop an ongoing experiment
