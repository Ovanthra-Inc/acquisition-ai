# 🧠 1. ARCHITECTURE (PHASE 2 EXTENSION)

You **DO NOT rewrite Phase 1**—you extend it.

```text
Existing:
Client → Gateway → Services → DB

Add:

                 ┌────────────────────┐
                 │ Enrichment Service │
                 └────────┬───────────┘
                          ↓
                 ┌────────────────────┐
                 │ Conversation Service│
                 └────────┬───────────┘
                          ↓
                 ┌────────────────────┐
                 │ Analytics Service  │
                 └────────┬───────────┘
                          ↓
                 ┌────────────────────┐
                 │ Notification Service│
                 └────────────────────┘
```

---

## 🔄 FLOW (INTELLIGENT OUTREACH)

```text
Agent → Lead Service → Enrichment Service
→ AI Service (analysis + scoring)
→ Campaign Service → Email Service
→ Conversation Service (reply tracking)
→ Analytics Service (metrics)
```

---

# 🧩 2. NEW / UPDATED SERVICES

---

## 🔍 1. ENRICHMENT SERVICE

### 🎯 Responsibility:

* Enhance lead data

### Features:

* Company description
* Industry detection
* Tech/use-case inference

---

## 💬 2. CONVERSATION SERVICE

### 🎯 Responsibility:

* Manage replies

### Features:

* Store email threads
* Fetch replies
* Reply classification

---

## 📊 3. ANALYTICS SERVICE

### 🎯 Responsibility:

* Track performance

### Features:

* Open rate
* Reply rate
* Conversion

---

## 🔔 4. NOTIFICATION SERVICE

### 🎯 Responsibility:

* Alert users

### Features:

* “New reply”
* “Follow-up needed”

---

---

# 🗄️ 3. DATABASE SCHEMA (PHASE 2 ADDITIONS)

---

## ENRICHED LEADS

```sql
ALTER TABLE leads ADD COLUMN industry TEXT;
ALTER TABLE leads ADD COLUMN description TEXT;
ALTER TABLE leads ADD COLUMN pain_points TEXT;
ALTER TABLE leads ADD COLUMN score INT DEFAULT 0;
```

---

## CONVERSATIONS

```sql
CREATE TABLE conversations (
    id UUID PRIMARY KEY,
    user_id UUID,
    lead_id UUID,
    thread_id TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);
```

---

## MESSAGES

```sql
CREATE TABLE messages (
    id UUID PRIMARY KEY,
    conversation_id UUID,
    sender TEXT,
    body TEXT,
    classified_as TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);
```

---

## ANALYTICS EVENTS

```sql
CREATE TABLE events (
    id UUID PRIMARY KEY,
    user_id UUID,
    type TEXT,
    metadata JSONB,
    created_at TIMESTAMP DEFAULT NOW()
);
```

---

---

# 🌐 4. NEW API ENDPOINTS (v1 EXTENSIONS)

---

## ENRICHMENT

```http
POST /api/v1/enrichment/enrich
POST /api/v1/enrichment/score
```

---

## CONVERSATIONS

```http
GET    /api/v1/conversations
GET    /api/v1/conversations/{id}
POST   /api/v1/conversations/classify
POST   /api/v1/conversations/reply
```

---

## ANALYTICS

```http
GET /api/v1/analytics/overview
GET /api/v1/analytics/campaign/{id}
```

---

## NOTIFICATIONS

```http
GET /api/v1/notifications
POST /api/v1/notifications/read
```

---

---

# 📁 5. FOLDER STRUCTURE (EXAMPLE: CONVERSATION SERVICE)

```text
conversation-service/
│
├── app/
│   ├── api/v1/endpoints/conversations.py
│   ├── models/
│   │   ├── conversation.py
│   │   └── message.py
│   ├── schemas/
│   │   ├── conversation.py
│   │   └── message.py
│   ├── repository/
│   │   ├── conversation_repo.py
│   │   └── message_repo.py
│   ├── services/
│   │   └── conversation_service.py
│   └── core/
```

---

---

# 🧱 6. SAMPLE CODE (PHASE 2)

---

## 🔹 models/message.py

```python
from sqlalchemy import Column, String, Text
from sqlalchemy.dialects.postgresql import UUID
import uuid
from app.db.base import Base

class Message(Base):
    __tablename__ = "messages"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    conversation_id = Column(UUID(as_uuid=True))
    sender = Column(String)
    body = Column(Text)
    classified_as = Column(String)
```

---

## 🔹 schemas/message.py

```python
from pydantic import BaseModel

class MessageCreate(BaseModel):
    conversation_id: str
    sender: str
    body: str

class MessageResponse(BaseModel):
    id: str
    body: str
    classified_as: str
```

---

## 🔹 repository/message_repo.py

```python
class MessageRepository:

    def create(self, db, data):
        msg = Message(**data.dict())
        db.add(msg)
        db.commit()
        db.refresh(msg)
        return msg
```

---

## 🔹 services/conversation_service.py

```python
class ConversationService:

    def classify(self, message_text):
        # call AI service
        if "interested" in message_text.lower():
            return "interested"
        return "neutral"
```

---

---

# 🤖 7. NEW AGENTS (PHASE 2)

---

## Lead Analyzer Agent

* Uses enrichment + AI

---

## Lead Scoring Agent

* Assigns priority

---

## Reply Detection Agent

* Pulls incoming replies

---

## Reply Classification Agent

* Categorizes responses

---

## Metrics Agent

* Tracks performance

---

---

# 🛠️ 8. NEW TOOLS

---

```text
enrich_lead
score_lead
match_icp
fetch_replies
classify_reply
get_metrics
track_event
```

---

---

# 🔄 9. QUEUE JOBS (NEW)

---

### Async tasks:

* Enrich lead batch
* Score leads
* Fetch replies (polling)
* Track email events

---

Example:

```python
@celery.task
def enrich_lead_task(lead_id):
    enrichment_service.process(lead_id)
```

---

---

# ⚡ 10. CACHING STRATEGY

---

Use Redis:

### Cache:

* Enriched lead data (TTL: 1h)
* Analytics queries (TTL: 5–10 min)

---

---

# 🔐 11. SECURITY ADDITIONS

---

### Add:

* Rate limit:

  * enrichment APIs (costly)
* Input sanitization for AI prompts
* Conversation data isolation (user_id strict)

---

---

# 📊 12. WHAT IMPROVES IN PHASE 2

---

| Area      | Phase 1 | Phase 2           |
| --------- | ------- | ----------------- |
| Leads     | Raw     | Enriched + scored |
| Emails    | Basic   | Context-aware     |
| Replies   | Ignored | Managed           |
| Analytics | None    | Full tracking     |

---

---

# 🔥 FINAL RESULT

Your system now:

👉 Finds better leads
👉 Sends smarter emails
👉 Understands replies
👉 Shows performance